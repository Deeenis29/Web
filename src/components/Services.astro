---
const leftColumnItems = [
  "Trastorno del espectro autista (TEA)",
  "Trastorno de déficit de atención e hiperactividad (TDAH)",
  "Síndrome de Down",
  "Problemas de lenguaje",
  "Problemas de aprendizaje"
];

const rightColumnItems = [
  { text: "Problemas de atención" },
  { text: "Problemas de conducta" },
  { text: "Problemas de concentración" },
  { text: "Estimulación temprana" },
  { 
    text: "Problemas psicológicos",
    subtext: "(Ansiedad, depresión, etc)"
  }
];
---

<section class="container mx-auto px-4 py-20">
  <div class="relative max-w-6xl mx-auto flex flex-col gap-16">
    <!-- Flechas decorativas -->
    <img 
      src="/flechita1.png" 
      alt="Flecha decorativa izquierda" 
      class="absolute left-0 top-1/3 -translate-y-[220px] -translate-x-[240px] w-2/7 h-3/5"
    />
    <img 
      src="/flechita2.png"
      alt="Flecha decorativa derecha" 
      class="absolute right-0 top-1/3 -translate-y-[240px] translate-x-[190px] w-2/7 h-3/5"
    />

    <header class="text-center space-y-4">
      <h2 class="text-[#3689D9] text-3xl font-bold font-nunito mb-10">¿Cómo podemos ayudarte?</h2>
      <p class="text-secondary text-xl max-w-[46em] mx-auto leading-normal">
        Nuestro equipo multidisciplinario se dedica a convertir 
        <span class="text-[#3689D9] font-semibold">desafíos en logros</span>, 
        apoyando a bebés prematuros y niños con diversas condiciones como:
      </p>
    </header>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-x-4 md:gap-x-8 lg:gap-x-20 gap-y-6 font-bold font-nunito">
      <!-- Columna izquierda -->
      <ul class="space-y-6 w-[120%]">
        {leftColumnItems.map((item) => (
          <li class="flex items-center gap-3">
            <img src="/button.png" alt="" class="w-10 h-10 flex-shrink-0" aria-hidden="true" />
            <span class="text-[#3689D9] text-xl">{item}</span>
          </li>
        ))}
      </ul>

      <!-- Columna derecha -->
      <ul class="space-y-6 ml-[30%]">
        {rightColumnItems.map((item, index) => (
          <li class="flex items-start gap-3">
            <img src="/button.png" alt="" class="w-10 h-10 flex-shrink-0" aria-hidden="true" />
            <div class={`flex flex-col ${index === rightColumnItems.length - 1 ? 'items-start text-center w-full' : ''}`}>
              <span class="text-[#3689D9] text-xl mt-1.5">{item.text}</span>
              {item.subtext && (
                <span class="text-[#3689D9] text-xl">{item.subtext}</span>
              )}
            </div>
          </li>
        ))}
      </ul>
    </div>

    <div class="text-center">
      <a 
        href="#servicios" 
        class="inline-block bg-[#FDB813] text-white px-12 py-3 rounded-full font-bold text-2xl font-nunito hover:bg-[#FDB813]/90 transition-colors"
      >
        Nuestros servicios
      </a>
    </div>
  </div>
</section>